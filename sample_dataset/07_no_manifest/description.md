# No manifest

CSV and notes are passed directly and merge through their shared email. This folder intentionally has no manifest.

```bash
python -m candidate_transformer --csv sample_dataset/07_no_manifest/applications.csv --note sample_dataset/07_no_manifest/notes.txt --config sample_dataset/07_no_manifest/config.json
```
