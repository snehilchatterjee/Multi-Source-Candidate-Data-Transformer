# Missing recruiter CSV

No recruiter CSV is supplied. The candidate is built entirely from recruiter notes, while note-only email and phone values remain secondary rather than being promoted to primary contacts.

```bash
python -m candidate_transformer --manifest sample_dataset/09_missing_csv/manifest.json --config sample_dataset/09_missing_csv/config.json
```
