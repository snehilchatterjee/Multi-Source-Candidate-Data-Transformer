# Multiple candidates

Four CSV rows resolve into three candidates with separate notes and skills.

```bash
python -m candidate_transformer --manifest sample_dataset/05_batch/manifest.json --config sample_dataset/05_batch/config.json
```
