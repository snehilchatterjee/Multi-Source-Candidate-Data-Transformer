# Missing IDs

Rows without candidate/application IDs merge through email and GitHub. Company aliases combine into one experience.

```bash
python -m candidate_transformer --manifest sample_dataset/06_missing_ids/manifest.json --config sample_dataset/06_missing_ids/config.json
```
